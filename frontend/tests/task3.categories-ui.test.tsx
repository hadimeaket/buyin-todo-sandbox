import { describe, it } from "vitest";

describe("TASK3_CATEGORIES_UI", () => {
  it.todo("renders a category badge with the configured HEX color");
  it.todo("allows creating, editing, and deleting categories from the UI");
  it.todo("validates that category colors follow the #RRGGBB format");
});
