import { describe, it } from "@jest/globals";

describe("TASK8_ICS_EXPORT", () => {
  it.todo(
    "generates an ICS file with DTSTART/DTEND populated from todo due dates"
  );

  it.todo(
    "names the file todos-YYYY-MM-DD.ics and serves it with text/calendar"
  );
});
