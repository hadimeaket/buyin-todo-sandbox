import { describe, it } from "@jest/globals";

describe("TASK6_EMAIL_VERIFICATION", () => {
  it("creates a verification token during registration and logs the link to stdout", () => {
    throw new Error(
      "NOT IMPLEMENTED: Email verification token generation not yet implemented"
    );
  });

  it("rejects login attempts until the verification endpoint is called", () => {
    throw new Error(
      "NOT IMPLEMENTED: Email verification check not yet implemented"
    );
  });

  it("expires verification tokens after 24 hours", () => {
    throw new Error(
      "NOT IMPLEMENTED: Token expiration logic not yet implemented"
    );
  });
});
